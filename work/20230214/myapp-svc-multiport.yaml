apiVersion: v1
kind: Service
metadata:
  name: myapp-svc-multiport
spec:
  selector:
    app: myapp-rS
  ports:
  - name: myapp-http
    port: 80
    targetPort: 8080
  - name: myapp-https
    port: 443
    targetPort: 8443
